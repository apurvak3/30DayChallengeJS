//Activity2
/**
 * task3:use the push method to add a new member to the end of the array and log the updated array.
 * task4:use the pop method to remove the  last member of the array and log the updated array.
 * task5:use the shift method to remove the first element from the array and log the updated  array.
 * task6:use the unshift method to add a new number to the beggining of the array and log the updatded array.
 */

const arr2 = [2,3,4,5,6];
arr2.push(8);
arr2.pop();
arr2.shift();
arr2.unshift();
console.log(arr2);
